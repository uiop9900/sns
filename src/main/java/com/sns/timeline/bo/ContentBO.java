package com.sns.timeline.bo;

import org.springframework.stereotype.Service;

@Service
public class ContentBO {

	/*
	 * @Autowired private PostBO postBO;
	 * 
	 * @Autowired private CommentBO commentBO;
	 * 
	 * @Autowired private LikeBO likeBO;
	 */
	// 객체가공할때 ->generate, get/ 다른패키지의 bo,dao부를수있다.
	// 로그인되지않아도 타임라인은 볼 수 있으므로 userId는 Integer이다.
//	public List<ContentView> generateContentViewList(Integer userId) {
		//반복문을 돌려서 글 리스트(postId)를 가지고 오고 
		//postId를 통해 comment를 가지고오고
		// 그 가져온 comment를 contentview에 넣고 가져온다?^^...
	}
//}
